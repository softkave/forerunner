# forerunner

- mongo invoker

- persist error files
- revert
- tags on client
- sync files on client
- handling errors during transition
- tests
- can we update node_modules while a program is running?
- only download new and updated files
- delete stale files?
- one-click deploy
- build somewhere else and copy build so each instance will have it's cwd
- check last updated to skip some files